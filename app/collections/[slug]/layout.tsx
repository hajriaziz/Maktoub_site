export const metadata = {
  title: "Collection - MAKTOUB",
  description: "Section Collection",
}

export default function AdminLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>
}