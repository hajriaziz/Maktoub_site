export const metadata = {
  title: "Your Shopping Cart",
  description: "panier des commandes",
}

export default function AdminLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>
}